services:
  app:
    build: .
    container_name: health-me
    ports:
      - '127.0.0.1:3002:3002'
    env_file: .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
